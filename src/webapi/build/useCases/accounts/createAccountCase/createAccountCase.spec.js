"use strict";
/* eslint-disable no-undef */
var assert = require('assert');
describe('createAccountCase', function () {
    it('should return ok', function () {
        assert.equal(1 + 1, 2);
    });
});
